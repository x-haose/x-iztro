## 改动

<!-- 这次做了什么、为什么。给 reviewer 看的。 -->

## 验证

<!-- 跑了哪些、结果如何。例如：cargo test 149 passed / go test ok / pytest 161 passed -->

## 发布说明

<!--
这一节会原样成为 GitHub Release 的描述，所以请按面向使用者的口吻写，
不要写实现细节和内部文件名。改了 Cargo.toml 版本号才会触发发版；
没改版本号的 PR 这一节留空即可（不会发版，也不会报错）。

写法示例：

### 新功能
- Python / Go 补齐 9 个安星函数：左辅右弼、火星铃星、华盖咸池……

### 修复
- `key_of` 反查同形译名时取值与 iztro 不一致（en-US 的 `horse` 等 8 处）

### 其他
- 文档站新增「与 iztro 的对应」一页
-->
