[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "x-iztro"
version = "0.1.0"
description = "紫微斗数 Rust 核心库 Python 绑定"
requires-python = ">=3.10"
dependencies = []

[tool.maturin]
features = ["python"]
python-source = "python"
module-name = "x_iztro._x_iztro"
